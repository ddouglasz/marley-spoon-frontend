import * as children from './children'
import * as props from './props'
import * as state from './states'
import * as mockedEntries from './data'

const FIXTURES = {
  mockedEntries,
  children,
  props,
  state
}

export default FIXTURES
